{% extends 'base.html.twig' %}

{% block title %}Ajouter des vêtements{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
     {{ form_start(form) }}
    <div >
        {{ form_errors(form) }}
    </div>

    <div class="row">
        <div class="col">
            {{ form_row(form.name) }}
        </div>
        <div class="col" >
            {{ form_row(form.prix) }}
        </div>
    </div>
    <div class="row mt-3">
        <div class="col">
            {{ form_row(form.categorie) }}
        </div>
        <div class="col" >
            {{ form_row(form.marque) }}
        </div>
    </div>
    <div class="row mt-3">
        <div class="col">
            {{ form_row(form.tailles) }}
        </div>
        <div class="col" >
            {{ form_row(form.imageUrl) }}
        </div>
    </div>
    <div class="row mt-3">
        <div class="col">
            {{ form_row(form.commentaire) }}
        </div>
    </div>
    <button type="submit"  class="btn btn-primary row mt-3">Submit</button>

{{ form_end(form) }}
</div>
<div>
</div>
{% endblock %}
