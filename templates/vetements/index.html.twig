{% extends 'base.html.twig' %}

{% block title %}Hello VetementsController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="container text-center ">
    <div class="row justify-content-center"> 
        <div class="col-12">
            <h1>Les vetements</h1>
        </div>
    {% for item in listeVetements %}
        <div class="col-5">
            <div class="col-12">
                <img src="{{ item.imageUrl }}" alt="image" width="200" height="200">
            </div>
            <div class="col-12">
                {{ item.name }}
            </div>
            <div class="col-12">
            
                {{ item.prix }}€ 
                <button type="button" class="btn btn-dark">Voir</button>
            </div>
        </div>
        
    {% endfor %}
    </div>
</div>
<div>
</div>
{% endblock %}
